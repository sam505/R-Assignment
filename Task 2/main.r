student.math <- read.csv("student-mat.csv",
                           sep = ";",
                           header = T
)

student.por <- read.csv("student-por.csv",
                          sep = ";",
                          header = T
)
View(student.math)
